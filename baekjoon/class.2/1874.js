// let input = require("fs").readFileSync("/dev/stdin").toString().trim().split("\n");
let input = require("fs")
  .readFileSync("data.txt")
  .toString()
  .trim()
  .split("\n");

let baseNum = input.shift(0);
arr = input.map((item) => Number(item));
let answer = [];

for (let i = 0; i < input.length; i++) {
  input[i];
}
